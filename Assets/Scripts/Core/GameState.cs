namespace CardGame.Core
{
    public enum GameState
    {
        Menu,
        Initialization,
        TurnStart,
        PassDevice,
        WaitingForInput,
        Resolution,
        TurnEnd,
        GameOver
    }
}
